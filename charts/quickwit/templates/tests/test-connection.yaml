apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quickwit.fullname" . }}-test-connection"
  labels:
    {{- include "quickwit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quickwit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
